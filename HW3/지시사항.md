주의사항: 생성형 AI 를 사용하여 과제 수행.

2개 (GPT, Gemini, Claude등 임의로 선정)를 사용하여, 각 문제의 **해답** 및 **핵심기능요약, 문제출제의도** 및 이를 얻기 위한, **입력 프롬프트**와 **결과**, **결과분석**, **개인적인 사용 소감** 작성

## Home Work #3

1. Implement insertion sort, selection sort, heap sort, merge sort and quick sort (In-place). Then, compare the running time of each sorting algorithm with python built-in sort() function using the following given file (dataset.zip). To measure the running time, use python package "numpy".

Data files in "dataset.zip" and the size of data:

- test1.dat (500)
- test2.dat (1,000)
- test3.dat (5,000)
- test4.dat (10,000)
- test5.dat (100,000)

(Student's ID and Name should be printed in the output.)

- Guide code (index.py) 골격: `insertion_sort / selection_sort / heap_sort / merge_sort / quick_sort / built_in_sort` 함수 + `data_files` 루프.
- Example output: 행=selection/heap/insertion/quick/merge/python, 열=500/1K/5K/10K/100K, 셀=`0.00ms` 형식.
